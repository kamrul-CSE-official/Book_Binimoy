export interface IProduct {
  _id: string;
  price: string;
  name: string;
  stock: number;
  author: string;
  edition: number;
  publishedDate: string;
  pages: number;
  rating: number;
  details: string;
  img: string;
  condition: string;
  status: boolean;
  quantity: number;
}
